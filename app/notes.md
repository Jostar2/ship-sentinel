# App Notes

Current app scope:

- static SPA shell
- in-memory demo state
- route-like view switching
- QA session as the main working surface
- intake, overview, maintenance, and deliverables as supporting views

Next implementation slice:

- persist local state
- editable fields on intake and memo
- richer capture inbox actions
- change request lifecycle transitions
- export integration from app state
