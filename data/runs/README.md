# Runs

`ship-sentinel/data/runs`는 파일 기반 감사 런 저장소다.

생성 방법:

- 앱에서 새 감사 생성 후 JSON 저장
- `python ship-sentinel\scripts\create_audit_run.py`
- `python ship-sentinel\scripts\create_audit_run.py --from-json <path>`
